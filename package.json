{
  "name": "daily-sticky-note",
  "productName": "Daily Sticky Note",
  "version": "1.6.1",
  "description": "A tiny always-on-top sticky note task list. Local-first by default, with optional cloud sync via your own Firebase project.",
  "author": "amarcu",
  "license": "MIT",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon build/icon.png",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
